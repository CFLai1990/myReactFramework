const rootFontSize = '10px' // The root font size is set in index.html
const sizeSuffix = 'rem'

const pageStyle = {
  minWidth: 80,
  minHeight: 45
}

const headerStyle = {
  viewHeight: 6, // vh
  minHeight: 5
}

const bodyStyle = {
  viewHeight: 90, // vh
  viewWidth: 98, // vw
  minWidth: 80,
  minHeight: 40
}

export { sizeSuffix, pageStyle, headerStyle, bodyStyle, rootFontSize }
