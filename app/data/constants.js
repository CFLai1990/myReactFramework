// Default dataset
const DEFAULT_DATA = 'Local CSV'

// Preload the local data
import LOCAL_CSV from 'data/Test_CSV.csv'
import LOCAL_JSON from 'data/Test_JSON.json'
import LOCAL_XML from 'data/Test_XML.xml'

export default { DEFAULT_DATA, LOCAL_CSV, LOCAL_JSON, LOCAL_XML }
